interface:
  display_name: "Task Track"
  short_description: "Repo-specific patterns and workflows for task-track"
  default_prompt: "Use the task-track repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true